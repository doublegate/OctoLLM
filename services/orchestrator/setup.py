"""Setup configuration for OctoLLM Orchestrator service."""

from setuptools import find_packages, setup

setup(
    name="octollm-orchestrator",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
)
