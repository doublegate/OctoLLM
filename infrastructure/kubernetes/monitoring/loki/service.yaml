apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: octollm-monitoring
  labels:
    app: loki
    component: monitoring
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      protocol: TCP
  selector:
    app: loki
