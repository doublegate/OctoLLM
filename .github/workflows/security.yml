name: Security Scan

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Set minimal required permissions
permissions:
  contents: read
  security-events: write

jobs:
  sast-bandit:
    name: SAST - Bandit (Python)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Set up Python 3.14
        uses: actions/setup-python@v7
        with:
          python-version: '3.14'

      - name: Install Bandit
        run: pip install bandit[toml]

      - name: Run Bandit security linter
        run: |
          echo "Running Bandit SAST scanner..."
          bandit -r services/orchestrator/ services/arms/ -f json -o bandit-report.json || true
          bandit -r services/orchestrator/ services/arms/ -ll -ii
        continue-on-error: false

      - name: Upload Bandit report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: bandit-report
          path: bandit-report.json
          retention-days: 30

  dependency-scan-python:
    name: Dependency Scan - Snyk (Python)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      # python-3.12, not 3.14, while the rest of this file is on 3.14: snyk/actions
      # publishes no 3.13 or 3.14 image (python-3.12 is the newest variant in the repo).
      # Harmless here -- Snyk reads pyproject.toml and poetry.lock, not the interpreter.
      # Bump when snyk ships a newer image.
      - name: Run Snyk to check for vulnerabilities
        uses: snyk/actions/python-3.12@master
        continue-on-error: true  # Don't fail build on vulnerabilities yet
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high --all-projects --sarif-file-output=snyk.sarif

      - name: Upload Snyk results to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: snyk.sarif
        continue-on-error: true  # Don't fail if SARIF file not generated

  dependency-scan-rust:
    name: Dependency Scan - cargo-audit (Rust)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      # actions-rs/* was archived in 2023 and no longer receives Node-runtime or
      # security updates; setup-rust-toolchain is the maintained replacement and is
      # what the lint/test workflows already use.
      - name: Set up Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v2
        with:
          toolchain: stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run cargo audit - Reflex Layer
        working-directory: services/reflex-layer
        run: |
          echo "Auditing Reflex Layer dependencies..."
          cargo audit --json > ../../reflex-audit.json || true
          cargo audit
        continue-on-error: true

      - name: Run cargo audit - Executor Arm
        working-directory: services/arms/executor
        run: |
          echo "Auditing Executor Arm dependencies..."
          cargo audit --json > ../../executor-audit.json || true
          cargo audit
        continue-on-error: true

      - name: Upload audit reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: cargo-audit-reports
          path: |
            reflex-audit.json
            executor-audit.json
          retention-days: 30

  container-scan:
    name: Container Scan - Trivy
    runs-on: ubuntu-latest
    # The Dockerfiles referenced below all exist. This stays disabled only because it
    # builds four images on every push; enable it when the CI budget allows.
    if: false  # Not enabled yet - see note above
    strategy:
      matrix:
        service: [orchestrator, reflex-layer, planner, executor]

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Build Docker image
        run: |
          echo "Building ${{ matrix.service }} image..."
          if [ "${{ matrix.service }}" = "orchestrator" ]; then
            docker build -t octollm/${{ matrix.service }}:latest -f services/orchestrator/Dockerfile .
          elif [ "${{ matrix.service }}" = "reflex-layer" ]; then
            docker build -t octollm/${{ matrix.service }}:latest -f services/reflex-layer/Dockerfile .
          elif [ "${{ matrix.service }}" = "executor" ]; then
            docker build -t octollm/${{ matrix.service }}:latest -f services/arms/executor/Dockerfile .
          else
            docker build -t octollm/${{ matrix.service }}:latest -f services/arms/${{ matrix.service }}/Dockerfile .
          fi

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@0.36.0
        with:
          image-ref: octollm/${{ matrix.service }}:latest
          format: 'sarif'
          output: 'trivy-${{ matrix.service }}.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-${{ matrix.service }}.sarif'

  secret-scan:
    name: Secret Scan - gitleaks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Full history for gitleaks

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  security-summary:
    name: Security Summary
    runs-on: ubuntu-latest
    needs: [sast-bandit, dependency-scan-python, dependency-scan-rust, secret-scan]
    if: always()

    steps:
      - name: Check security scan results
        run: |
          echo "✅ Security scan workflow complete"
          echo "Review Security tab for detailed results: https://github.com/${{ github.repository }}/security"
          echo ""
          echo "Phase 0 Active Scans:"
          echo "- ✅ Bandit (SAST): Scanning Python code for security issues"
          echo "- ✅ Snyk: Checking Python dependencies for vulnerabilities"
          echo "- ✅ cargo-audit: Auditing Rust dependencies"
          echo "- ✅ gitleaks: Scanning for exposed secrets"
          echo "- ⏸️  Trivy (Container): Disabled (build cost), not missing Dockerfiles"
          echo ""
          echo "Expected Results:"
          echo "- Bandit: Clean (minimal code in Phase 0)"
          echo "- Snyk: Some dependency warnings (acceptable)"
          echo "- cargo-audit: Clean"
          echo "- gitleaks: Clean (no secrets)"
